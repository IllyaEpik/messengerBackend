model User {
    id Int @id @unique @default(autoincrement())
    email  String @unique   
    password String 
    addedAt DateTime @default(now())
    // profileId Int? @unique
    profile Profile? @relation("profile_user")


    posts Post[]

    likes Like[]

    loves Love[]

    views View[]

    friendShipsInvate FriendShip[] @relation("inviter")

    friendShipsAccept FriendShip[] @relation("accepter")

    admins Admin[]

    messages Message[]
    members Member[]
    code Code?
    confirmedUser Boolean @default(false)
}