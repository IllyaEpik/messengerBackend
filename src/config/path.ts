import { join } from "node:path";
export const uploadDir = join(__dirname, "../", "../", "./media");
export const thumbDir = join(uploadDir, "./crackedAvatars");
export const originalDir = join(uploadDir, "./Avatars");